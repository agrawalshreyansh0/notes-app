pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building...'
                // Add build commands here (e.g., `./gradlew build` for Java, `npm install` for Node.js)
            }
        }

        stage('Test') {
            steps {
                echo 'Testing...'
                // Add test commands here (e.g., `./gradlew test` for Java, `npm test` for Node.js)
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deploying...'
                // Add deploy commands here (e.g., copying files, running scripts)
            }
        }
    }
}
